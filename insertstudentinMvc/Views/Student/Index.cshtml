@model insertstudentinMvc.Models.StudentReg
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        @* @using (Html.BeginForm) { } *@
        @{
  Html.BeginForm("Index", "Student", FormMethod.Post);

        @Html.LabelFor(Model => Model.Id) @Html.TextBoxFor(Model => Model.Id)<br />
        @Html.LabelFor(Model => Model.Name) @Html.TextBoxFor(Model => Model.Name)<br />
        @Html.LabelFor(Model => Model.Class) @Html.TextBoxFor(Model => Model.Class)<br />
        <br />
        <input type="Submit" value="Save" />
            }


    </div>
</body>
</html>
